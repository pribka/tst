from django.urls import path
from . import views

app_name = 'ehub'

urlpatterns = [
    # path('register/', views.EhubRegisterView.as_view(), name='ehub-register'),
    # path('redirect/<str:code>/', views.RedirectToServerView.as_view(), name='redirect-to-server'),
    # path('servers/', views.ServerListView.as_view(), name='server-list'),
    # path('regions/', views.EhubRegionModelListView.as_view(), name='region-list'),
    # path('alma_maters/', views.AlmaMaterListView.as_view(), name='alma-mater-list'),
    # path('positions/', views.PositionListView.as_view(), name='position-list'),
    # path('languages/', views.LanguageListView.as_view(), name='language-list'),
    # path('sexes/', views.SexListView.as_view(), name='sex-list'),
    # path('avatar/', views.GetAvatarView.as_view(), name='avatar-view'),
    # # по токену
    # path('chat_info/', views.ChatInfoView.as_view(), name='ehub-chat-info'),

]
